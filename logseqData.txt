// linux
script session.log # write cmd and it's output to a log file
exit # exit the session

// vim
za // toggle fold unfold current block
zc (close fold): 

    This command closes the current fold (collapses it). 

3. zo (open fold): 

    This command opens the current fold (expands it). 

4. za (toggle fold): 

    This command toggles the current fold, opening it if it's closed and closing it if it's open. 

5. zM (close all folds): 

    This command closes all folds in the current buffer. 

6. zR (open all folds): 

    This command opens all folds in the current buff




// // git
// install git-credential-manager
147  wget https://aka.ms/gcm/linux-install-source.sh
  148  chmod +x linux-install-source.sh
  149  ./linux-install-source.sh


git config --global credential.credentialStore secretservice
  
159  git pull origin main

// ssh
ssh-keygen -t ed25519 -C "your_email@example.com" // generate key

eval "$(ssh-agent -s)"
ssh-add GitSSHAuthentication/key05

cat ~/.ssh/id_ed25519.pub

GITHUB SSH add key

  183  ssh -T git@github.com // check connection
