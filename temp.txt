Dagster sensors failed due to a temporary 502 Bad Gateway error from the LSEG API during authentication, which resolved automatically with no downstream impact.

JWT authentication failures occurred due to a public key fingerprint mismatch caused by unsynchronised key rotation, and self-healed after the scheduled key sync job.

Dagster alert triggered due to a deployment issue pulling an incorrect container image in prod, which was resolved after correcting the configuration.

Access to dev and staging environments was requested, implemented via MR, and successfully validated.

CI jobs were stuck due to unavailable GitLab runners matching required tags, and resolved after restoring runner availability.

Confluence knowledge base was updated with closed tickets and technical summaries to improve documentation and knowledge sharing.

Missing data in the Legacy Data Warehouse monthly run was corrected using staging-to-published table swaps and validated as accurate.

Dagster alert triggered as a job exceeded the 1-hour runtime threshold by around 36 seconds with no functional impact.

Lambda failure in staging during Snowflake user key rotation was likely due to rate limiting or network policy issues, with recommendation to add retry handling.

User lost access to a Snowflake role due to expiry of temporary access and AAD provisioning revocation, with a new request raised for restoration.

Datadog alert triggered due to a DMS CDC error caused by an unsupported RENAME TABLE operation and initial investigation in the wrong environment.

Pipeline failed due to missing access to SNOWFLAKE.ACCOUNT\_USAGE schema, resolved by granting required permissions via IaC.

Weekly Jira dashboard reporting process was created using JQL filters and CSV exports for trend tracking.

ICMR dashboard data mismatches caused by timing gaps and CDC latency were resolved through data reload and deduplication fixes.

Snowflake connection issue occurred in GitLab pipeline but worked via Lambda, with investigation focused on user configuration and network policies.

Multiple Snowflake access and permission issues were resolved by creating a read-only data platform role and addressing related gaps.
